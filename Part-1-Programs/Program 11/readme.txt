Q. Create an event listener for TicketManager Javascript class that allows a user to buy tickets. Set up listeners for the for the 'buy' event, which will trigger every time a ticket is bought. The process should show how to manage erroneous events from the emitter and how to manage event subscribers.